# Aitoapi Custom

这是由 `mulan777` 维护的二开项目。

## 维护文档

后续功能追加、故障修复、重构和发布前，请先参阅 [`docs/zh/project-maintenance-guide.md`](docs/zh/project-maintenance-guide.md)。该文档以当前源码为基线，说明模块边界、请求链路、多账号调度、熔断恢复、配置持久化、测试矩阵和文档同步规则。

## 项目来源与许可证

本项目 `mulan777/Aitoapi-custom` 基于 [iBUHub/AIStudioToAPI](https://github.com/iBUHub/AIStudioToAPI) 和 [Ellinav/ais2api](https://github.com/Ellinav/ais2api) 进行二次开发；感谢两位上游作者和贡献者。本项目及其中来自上游的代码沿用 **CC BY-NC 4.0** 许可证。非商业复制、修改和分发必须保留原作者署名、上游链接和许可证声明，并标注本项目的修改内容；不得暗示上游作者认可或背书。商业用途请先取得相应权利人的许可，完整条款见 [`LICENSE`](LICENSE)。

## 二开内容

- 扩展模型列表配置，支持更多模型元数据与模型别名。
- 支持模型名后缀组合，用于思考等级、流式模式、联网搜索和代码执行等能力选择。
- 恢复模型发现接口中的后缀变体，例如 `gemini-3-flash-preview-high-fake-search`；这些变体与直接请求时的后缀解析保持一致。
- 增强模型发现与请求转换逻辑。
- 增加思考等级与 `includeThoughts` / 输出配置的兼容处理。
- 修复多账号并发请求的全局账号漂移：请求会绑定到一个活跃账号连接并轮询分配，重试也保持请求级路由；保活器现在按账号上下文独立运行。将 `MAX_CONTEXTS` 设置为 `2` 或更多即可同时保持多个账号在线。
- `BackgroundWakeup / Rocket Handler` 也已改为按账号独立运行；切换当前账号、单账号重连或关闭某个账号时，不会停止其他账号的保活。
- 保留上游的 OpenAI、Gemini、Anthropic 兼容接口和 Web 控制台。
- 凭证支持手动禁用/启用；上游返回配置的状态码（默认 `401,403`）时自动禁用该凭证，避免继续轮询坏账号。
- 控制台可持久化修改最大重试次数、重试间隔、自动禁用状态码、并发上下文数和 429 冷却参数。
- 429 会记录“凭证 + 原始模型”的持久化冷却信息；当前版本同时启用账号级 quota 熔断，单次额度型 429 会暂时禁用该凭证并交由 AutoHeal 隔离探测恢复。
- 每个凭证显示当日按模型拆分的成功/失败次数。
- 控制台提供独立“模型探测”页面，可按已启用账号顺序手动检测 17 个文本基础模型和 7 个图像基础模型；结果原子保存到 `data/model-probes.json`，不会改变 `/v1/models` 的静态完整目录。
- 账号替换改为原子流程：新上下文和 WebSocket 成功后才关闭旧槽位，避免 429/20 次轮换触发浏览器反复重建。

## 多账号轮询

当前实现支持类似“5 个账号同时在线、请求轮询分配”的模式：

1. 将 5 份登录后的认证文件放入 `configs/auth/`。
2. 在项目根目录的 `.env` 中设置：

   ```dotenv
   MAX_CONTEXTS=5
   ```

3. 启动服务：

   ```powershell
   npm run build:ui
   npm start
   ```

启动时第一个账号先就绪，其余账号在后台依次加载；加载完成后，只有已建立 WebSocket 的账号会进入轮询池。每个请求只分配给一个账号，流式响应和重试继续绑定原请求账号，不会把同一请求复制发送给 5 个账号。

`MAX_CONTEXTS` 默认仍为 `1`，设置为 `5` 才会同时维持最多 5 个账号上下文。每个账号都需要独立浏览器上下文和 WebSocket，因此内存占用会明显增加。

### 429 和请求调度

- 普通轮询只在“同一优先级下”使用轮询；有长流式请求时优先选择当前在途请求更少的账号，避免把请求继续堆到一个账号上。
- 某个账号的某个原始模型返回 HTTP 429 后，系统先记录该“账号 + 模型”的冷却与退避状态；基础冷却默认 5 分钟，最长 30 分钟，可用 `ACCOUNT_COOLDOWN_MS` 和 `ACCOUNT_COOLDOWN_MAX_MS` 调整。
- 当前版本还会触发账号级 quota 熔断：凭证暂时标记为 `quota_exhausted`、退出轮询并关闭上下文；默认 20 分钟后由 AutoHeal 使用池外隔离浏览器探测，健康后自动恢复。
- 账号列表中的“测试”按钮只检查该账号的 Auth、浏览器上下文和 WebSocket，不发送模型生成内容；测试不会清除仍未到期的模型冷却。完整语义见维护基线文档。
- 默认 `AUTO_DISABLE_STATUS_CODES=401,403`；自动禁用后需要在凭证列表手动启用，避免无权限账号反复占用槽位。

## 关于 build 和保活

`npm run build:ui` 只负责构建前端静态资源，不是保活进程。真正保持浏览器上下文、WebSocket 和账号保活的是持续运行的 `npm start` 进程；关闭终端或结束 Node 进程后，5 个浏览器上下文都会停止。Windows 部署建议把 `npm start` 注册为任务计划程序或服务；Docker 部署使用 `--restart unless-stopped`，即可在容器异常退出后自动拉起。

## 安全说明

仓库不包含任何 Google 登录凭据、Auth JSON、API Key、控制台密码、代理凭据或运行统计数据。部署时请通过环境变量配置密钥，并将 `auth/`、`data/` 作为本地持久化目录挂载。Zeabur 需要把持久卷挂载到 `/app/data`，否则模型探测结果会随容器重建而丢失；此功能不需要新增环境变量。

## 许可证

本项目沿用上游项目的 CC BY-NC 4.0 许可证。上游来源、许可证全文和原始版权声明请见仓库中的 `LICENSE` 文件。

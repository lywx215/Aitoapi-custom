# DIAG-03 R1 修订：待复审

项目、worktree、分支保持不变：Aitoapi-custom，`C:/Users/lywx2/.codex/worktrees/ca98/Aitoapi-custom`，`codex/diag-03-aito-diagnostics`。

任务基线：`daeab836eb194d4ff402bc3d645baeba4ccdc96f`；本次修订父提交（退回 HEAD）：`827a37bd463d258663d31d60e0802a356197677c`。新 HEAD 完整 SHA 见交付消息，避免提交哈希自引用。只读审查来源：`G:/code/gemini30/CLIProxyAPI/coordination/diagnostics/20260924/reviews/DIAG-03-R1/review.md`。已逐项读取；未自行调用 Claude 或委派任务。

冻结契约来源仍为 `bb291667f7b6bd7a1dab6f9b7f906b5871d1306c`，制品 `1.0.0-rc.1`；SHA256SUMS 摘要仍为 `ddb202238cfdaabef1af11575dbfcac788fdbc5a457aea5e72b913afc5b478d4`。目录中所有冻结字节均保持原样；新增用例在本仓库运行时测试内。

## P2 逐项处置

| 意见                                        | 处置及证据                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P2-1 error 丢失 HTTP status/误标 read_error | **采纳。** `ConnectionRegistry._observeDiagnosticMessage` 在原 socket/auth/request/attempt 校验之后，仅对 response_headers 或无 code/http_error 的 error 保存 100–599 整数状态码；network_error→transport_error，aborted→cancelled，read_timeout/invalid_utf8→read_error，其余 unknown。浏览器 `build.js:275–280` 对生成 HTTP 错误取消响应体，因此 http_error 不声称 EOF。`build.js:1059–1070` 对非 HTTP 异常的默认 504 不当作上游 HTTP 观测。真实 WS→Registry→Pipeline 回归涵盖 429/503、无 code 的429、network、abort、timeout、invalid_utf8、resource_exhausted、未知 code，共9类。 |
| P2-2 observeFrame fail-open                 | **采纳观察器保护，不采纳已证明新增业务故障的结论。** `GenerationResultGuard.observe:40–71` 已在观察器前拒绝候选非数组、null 候选和非数组 parts；本次未改 guard。观察器自身增加数组/元素检查和局部 catch；异常 accessor 下数值置 null。真实流程中三种畸形帧分别在 DEBUG/INFO 下运行，响应状态、正文及错误码相同；另有直接观察器异常测试。未引入通用 safe 框架、未重构业务。                                                                                                                                                                                                             |
| P2-3 close-before-finish 误归因客户端       | **采纳保守方案。** 正常 finish 为 finished；既有 `__generationDestroyed` 明确本地失败证据为 error/failed；其余提前 close 为 unknown/unknown，包括完整请求发送后的客户端断开。未包装 destroy，未修改关停/取消流程。真实 HTTP 测试分别覆盖主动 res.destroy、closeAllConnections 和带既有失败标记的 destroy；原取消测试也核对 unknown。                                                                                                                                                                                                                                                   |
| P2-4 attempt timing 混用                    | **采纳。** firstUpstreamByteMs/firstEffectiveOutputMs 存在 attemptId 对应观察对象，结束后删除；Pipeline 原观察点传实际 attemptId。所有 timing 字段的计时原点明确为本地 server span 开始的 performance.now()，表示该请求起点后的观测偏移；attempt.totalMs 仍是该 attempt 开始后的持续时间。responseCommitMs 保留请求级。四协议流/非流的空结果重试验证第二次 firstUpstreamByte 晚于第一次，第一次 effective=null；独立测试验证第二次未观测时两项均 null，不继承旧值。                                                                                                                    |
| P2-5 reasoningIncludedInOutput 缺证据       | **采纳补证据和实际转换断言，不改业务公式或现有正确标志。** `FormatConverter._parseUsage:2186–2210` 已明确 completion=candidates+thoughts；Responses 在1832/2149使用该 completion；Claude流在2800–2803、非流3032/3094也相加。新增87候选+13思考、总103的合成上游，四协议×真流/非流/伪流12组同时解析实际响应和公共投影：OpenAI Chat/Responses outputTotal=100，reasoning=13，包含标志=true；Claude output=100，但未交付独立 reasoning，保持null；Gemini候选87、思考13各自保留，outputTotal仍null。此标志是本服务转换器的证据，不授权对任意远程OpenAI服务推断。                            |
| P2-6 legacy/public 挤占终局容量             | **采纳独立计数方案。** 仍用原单个 diagnosticQueue；legacy 维持原96普通/128终局限额，public DEBUG独立96，public basic独立128，总上限352；出队/DEBUG清空同步减计数，public/legacy丢弃计数独立。压力测试在同一同步批次分别先填legacy和先填public，两种顺序均达到352并正确拒绝超额；128条basic终局全部输出，其中一组在flush前关闭DEBUG。没有新增通用队列。                                                                                                                                                                                                                                 |

## 协调方补充意见

- **冒号实际目标路径：采纳。** `Peers.pathValid` 区分配置 prefix 与实际 target 的语法。配置仍为冻结严格规则；实际 target 接受未转义 URI pchar（含模型方法冒号），保持百分号、反斜线、dot segment、重复斜线和 prefix 边界拒绝。配置 `/antigravity` 可匹配 `/antigravity/v1beta/models/gemini-3.7-flash:generateContent`，既有162条共享向量不变，另加本地真实 HTTP 用例和独立拒绝断言。
- **HttpBoundary 合法头形式和 sealed span：采纳。** 支持对象（包括数组值）、原生扁平数组，以及规整的二元数组；数组在 native 发送前转换成扁平序列，保留重复业务值和业务字段大小写。不再因 span sealed 直接透传：始终去掉 X-Diag-*；拥有权为true时去掉模块标准追踪头，未拥有的显式业务 traceparent 保留；sealed 时不注入、不创建新call。真实网络验证所有这些情况。
- **出站能力范围：维持。** HttpBoundary 仍无生产调用方，要求调用者提供已规范化的 protocol/hostname/port/path 和追踪头拥有权；不猜测缺省协议或绕过自定义 socket/connect。未接入浏览器 fetch、远程图片或版本查询，未新增请求/重定向策略。

## P3 逐项处置

| 意见                             | 处置及证据                                                                                                                                                                                                                                                             |
| -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P3-1 res error 监听改变异常语义  | **采纳。** 删除新增的 res.once(error)，通过原 finish/close 结算；真实响应测试断言诊断不会增加普通 error listener。                                                                                                                                                     |
| P3-2 writeHead 嵌套数组          | **采纳兼容性修复。** 合法二元数组在过滤X-Diag前规整成扁平数组；真实 HTTP 验证状态201、正文、业务头、两个Set-Cookie与本地追踪ID。仍需在提交时setHeader设置本地ID，未改变背压或提前提交。                                                                                |
| P3-3 访问日志路径排除            | **暂不排除，记录取舍。** 公共入口记录保留所有经过Express的请求，响应ID对应的终局不会因旧人类可读日志排除条件而静默缺失；与原访问文本日志过滤不同。高频管理轮询会增加公共日志量；可用启动配置 ACCESS_LOG_ENABLED=false 关闭基础日志。此取舍不表示所有路径都有细节覆盖。 |
| P3-4 非流 firstDownstream 死测量 | **采纳。** 删除 attempt结束后才写入、没有记录载体的测量；当前firstDownstreamEffectiveOutputMs均null。                                                                                                                                                                  |
| P3-5 已关闭响应登记holder        | **采纳。** 仅req.res存在且非destroyed、非writableEnded时绑定ID和登记map/清理监听；已关闭时不修改身份。真实destroy之后调用原_startTrackedRequest，断言不创建登记map。                                                                                                   |
| P3-6 转换器补零                  | **采纳文档和断言。** Responses上游usage缺失时，转换器实际返回output_tokens=0；deliveredUsage明确present=true/source=converted/value=0；上游candidate仍present=false/source=unknown/value=null。四协议原missing用例保留，Responses流/非流新增精确断言。                 |
| P3-7 package.json四空格          | **不回退。** 仓库现有ESLint要求四空格；此次仅追加专项测试脚本，无版本/依赖调整。                                                                                                                                                                                       |
| P3-8 worktree本机路径            | **不删除。** 这是任务要求的交付台账与契约来源审计位置；未加入公共运行日志，不含凭证。                                                                                                                                                                                  |
| P3-9 清单外多余文件              | **采纳。** verifyContract.js新增工作区及Git暂存路径全集与SHA256SUMS+清单文件自身的比对，既验证内容也验证成员集合。                                                                                                                                                     |
| P3-10 前导零端口                 | **不自行新增拒绝规则。** 冻结规范要求十进制有效端口及origin规范化，未要求拒绝前导零；保持已冻结语义，不改契约/向量。如需收紧应由协调方升级契约。                                                                                                                       |
| P3-11 HttpBoundary未来接口问题   | **采纳。** 数组/封存清理/大小写见上。ClientRequest和IncomingMessage错误观察改用Node errorMonitor，不增加普通error listener，保留调用方错误处理责任；测试验证普通error listener数为0。原有重定向复制也保留业务头大小写。无生产接入。                                    |
| P3-12 配置动态方法未接生产       | **采纳说明。** DIAG_PEERS/ACCESS_LOG_ENABLED生产只在启动时读取；reloadPeers/setAccessEnabled为显式方法，当前仅隔离测试调用，不是生产热更新接口。生产DEBUG仍沿原LoggingService动态变更；accessCapture interrupted的测试不代表生产已接入访问配置热更新。                 |

## 验证台账与样例

实际npm入口为 `node tmp/npm/package/bin/npm-cli.js`（本机npm不在PATH）；下表列出等价命令。Node v24.19.0、隔离Python 3.12.10，均在本worktree运行。所有用例使用loopback/合成数据/临时目录。

| 命令                                                                                                                                                                                                                                                                                      | 退出结果                                                                                                                 |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| `npm run test:diagnostics`                                                                                                                                                                                                                                                                | 0；162共享运行时向量、真实入口、106运行时分组（466记录）、双实例与重启12回复（51记录）、新增R1队列/边界回归（369记录）。 |
| `npm run test:generation`                                                                                                                                                                                                                                                                 | 0；107 pipeline场景及18 safety分组。                                                                                     |
| `npm run test:stream-integrity`                                                                                                                                                                                                                                                           | 0。                                                                                                                      |
| `npm run test:routing`                                                                                                                                                                                                                                                                    | 0。                                                                                                                      |
| `npm run test:management`                                                                                                                                                                                                                                                                 | 0；原LIVE-01真实账号门槛跳过，其余通过。                                                                                 |
| `tmp/diag-venv/Scripts/python.exe contracts/diagnostics/v1/validate.py`                                                                                                                                                                                                                   | 0；3schema、53fixture、9example、242冻结向量，原摘要不变。                                                               |
| `tmp/diag-venv/Scripts/python.exe scripts/diagnostics/validateRecords.py tmp/diagnostics-runtime.jsonl tmp/diagnostics-ingress.jsonl tmp/diagnostics-processes.jsonl tmp/diagnostics-revision.jsonl docs/diagnostics/synthetic-runtime.jsonl docs/diagnostics/synthetic-r1-runtime.jsonl` | 0；963条真实产生/原样摘录记录，通过schema、语义和4096字节检查。                                                          |
| `node scripts/diagnostics/verifyContract.js --index`                                                                                                                                                                                                                                      | 0；工作区和暂存区72文件及清单摘要/成员集合一致，无冻结文件修改。                                                         |
| `node node_modules/eslint/bin/eslint.js <本次全部变更JS和package.json>`                                                                                                                                                                                                                   | 0。                                                                                                                      |
| `node node_modules/prettier/bin/prettier.cjs --check <本次全部变更JS、package.json和交付文档>`                                                                                                                                                                                            | 0。                                                                                                                      |
| `git diff --check`、`git diff --cached --check`                                                                                                                                                                                                                                           | 0。                                                                                                                      |

新增 [synthetic-r1-runtime.jsonl](synthetic-r1-runtime.jsonl) 为实际专项输出的67行原样摘录：启动、12种87+13协议/交付组合、429/503及网络错误对应完整请求。没有手写替代运行记录；全量输出在忽略的tmp内。日志文件为 `tmp/r1-{diagnostics,generation,stream,routing,management}-final.log`。初次修订自测中的旧队列总长断言改为新独立额度；晚登记测试发现bind仍在关闭检查之前，已一起移入检查后，随后完整重跑通过。

隔离双实例启动继续使用 [初次交付报告](DIAG-03-delivery.zh-CN.md#diag-07-隔离启动)。新增可选模型：`diag-reasoning`（87+13）、`diag-http429`、`diag-http503`、`diag-legacy429`、`diag-network`、`diag-abort`、`diag-timeout`、`diag-invalidutf8`、`diag-resource`、`diag-unknownerror`、`diag-badcandidate`、`diag-badcandidates`、`diag-badparts`；reasoning的伪流使用 `diag-reasoning-fake`。

## 剩余边界与未执行动作

验证过程另记录一次外层命令退出异常：管理回归日志已完整输出 ALL TESTS PASSED 且无 FAILED，但该次工具返回退出1，原因未确认。未忽略该结果；同一代码再次运行并显式输出 `management npm exit: 0`，最终通过日志为 `tmp/r1-management-confirm.log`。上表管理回归结果采用这次明确退出0的复跑。

公共基础记录满128仍可丢弃；独立额度保证不被legacy/DEBUG挤占，不保证任何并发量下零丢弃。单队列上限由128增至352，增加有界排队内存和最坏flush轮次，单轮32条保持不变。缺少明确来源证据的提前close保持unknown；当前无法将所有真实客户端取消与本地关停分开。

生产覆盖仍为HTTP入口、服务端浏览器派发、attempt结果和转换usage；不包含浏览器内部HTTP/重定向、图片、VNC或normalization/throttle。转换后正文计数、firstDownstream有效输出时间、callerAlias、credentialRef等仍为null/unknown。没有改变业务取消、关停、队列/ACK、重试、协议转换或计费公式。

仅修改本任务原worktree，未改冻结契约、其他仓库或浏览器脚本；未读取真实凭证、运行真实账号/远程模型/生产数据库/Volume；未部署、推送、合并或自行Claude审核。提交新HEAD后等待协调方复审，旧审核不视为通过。

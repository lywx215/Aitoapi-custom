# 本地真实凭证验证记录 — 2026-09-22

## 结论

**部分通过；双账号真实模型全流程验收未通过，不能发布为“真实验证成功”。**

两份不同账号的加密文件均成功解密并通过真实管理 API 导入。两次隔离验证均停在 `identity_unconfirmed`，没有确认页面当前登录身份，没有发起模型生成，账号保持禁用。不能把“文件解密成功”“导入任务创建成功”或“页面 HTTP 200”算作账号可用。

本次捕获到的是 AI Studio 页面后台 XHR 的 401/403，来源为 `alkalimakersuite-pa.clients6.google.com`。这不是 `gemini-3.8-flash` 生成响应；两个验证结果的 `upstreamStatus` 均为 `null`。**不能判定目标模型返回了 403，更不能判定账号永久封禁。**

当前阻塞可定位为真实页面会话/身份确认阶段，但尚不能仅凭这些证据区分：凭证失效、页面初始化/权限问题，或现有身份字段适配不兼容。没有自动更换账号、重新登录、确认服务条款或降低身份校验。

## 环境与执行边界

- 基线 `9c39fd843bd4f32b4d4bebddf92ad215b0ac52cb`；分支 `codex/management-api-integration`。实际业务源码摘要记录在每次 `result.json`，包含本轮启动保护修复。
- 隔离工作区：`C:/Users/lywx2/.codex/worktrees/management-api-integration/Aitoapi-custom`。原工作区仍为 `d24d14ee97abb8e08599ee85fe4f6127906d8056`，原有修改保留。
- 实际 HTTP `127.0.0.1:7860`，生产 WebSocket 端口 9998。真实 ProxyServerSystem、Playwright、Camoufox、Google 页面及管理接口；没有替换模型结果或浏览器验证器。
- 使用 Camoufox `135.0.1-beta.24` Windows x86_64。仅下载/解压官方版本，未运行登录设置流程。
- 读取源项目已有 AES-256-GCM 文件，仅使用其解锁和 ArtifactStore 能力。未运行旧导入脚本、未访问其线上目标地址、未修改源文件或数据库。
- 不操作 GUI、不访问 Zeabur、不部署。只创建本次实例的临时密码、模型 Key 和独立管理 Key。
- 业务浏览器与隔离验证浏览器分别完成匿名网络预检，AI Studio 导航均 HTTP 200；这只证明能加载入口，不证明已登录。

## 账号固定与预检

按账号 ID 去重、每个账号取最新文件，再选最新两个不同账号；两次运行和诊断使用同一 A/B，没有因失败替换。

|标识|加密文件 SHA-256|Cookie / Origin 数量|主记录中的账号 ID|凭证/状态版本|
|---|---|---|---|---|
|A|`ce1ed1f270f428d8a6db994ba5d1e54a8d0532f4efc99691b33f537523a92f9b`|32 / 1|`0d062cd8-0b4e-4a62-97cc-780f3be7aaca`|1 / 1|
|B|`5e2d80d45465eee8ec5936f9299c7c500228972ede6d7c64910c20aecc444b50`|33 / 1|`0529ed35-bd2c-4050-9964-31cfc4e9c5ce`|1 / 1|

文件元数据、来源账号 ID、解密结构中的账号名与邮箱摘要一致，且 A/B 摘要不同。该校验只能确认文件内部一致性，不证明 Google 当前承认这些 Cookie。

## 时间线（Asia/Shanghai）

1. **15:52:18–16:02:47，`run-LgREXW`**：A 首次验证等待约 10 分钟仍未完成，入口报 `TASK_TIMEOUT` 后清理。该次没有继续 B，不能算双账号验证。生成尝试为 0。
2. 修正测试入口的截止时间竞争：到期先读取最终结果，再合作式取消并等待清理；单个失败账号不再阻断另一个固定账号的独立验证。
3. **16:12:29 完成的页面诊断**：两账号均遇到固定应用启动提示 “This app is from another developer”。仅在精确识别该提示和 “Continue to the app” 按钮后继续。应用壳显示 `AIStudioToAPI-V1.3.8`，预览 iframe 的可见正文长度为 0。顶层和所观察框架均未提供当前适配器依赖的 `WIZ_global_data.oPEP7c`。未点击登录、普通 Continue 或 Google 服务条款。
4. **16:15:21–16:17:41，`run-EGxBQI`**：带实际错误观测重新执行同一 A/B，各项等待显式设为 60 秒，仅用于已知阻塞的补充验证。两个导入任务分别取消，均保持禁用；之后完成真实服务重启检查和清理。该次没有修改服务验证器的默认 10 分钟时限或放宽成功标准。

整个测试及诊断观察到的模型生成尝试总数为 **0**，未消耗计划中的生成重试预算。页面后台初始化请求不计作生成请求。

## 逐项验收

|阶段|本次真实结果|限制或证据|
|---|---|---|
|加密文件预检|通过|A/B 解密、结构与内部身份一致性校验通过，源文件摘要复核未变|
|本地鉴权边界|通过|模型 Key 访问管理返回 401，管理 Key 访问 `/v1/models` 返回 401，未认证管理返回 401，只读 Key 写入返回 403；生成计数不变|
|首次导入|仅存储与任务准入通过，模型验证未通过|分别创建真实账号/任务；`identity_unconfirmed` 后取消，`success:false`，保持禁用|
|指定账号模型复验|未执行|首次验证失败，不继续该账号生成请求|
|自动启用|未发生，不能计成功|两账号 `enabled:false`；失败关闭符合保护要求|
|模型 API 非流式/流式|未执行|没有通过身份校验的目标账号；不能用其他账号替代|
|管理基础操作|部分通过|分页查询、逐项进度、同幂等键返回原 taskId且生成数不增；有效不同内容同键返回 409；非法输入返回 400|
|明确启用/禁用完整链路|未验收|未对验证失败账号强行启用；不得把基础管理检查等同于该项通过|
|连接隔离与时间重叠|未执行|依赖两个账号模型链路成功，本次条件不满足|
|同目录正常重启|通过本次检查|原管理 Key 可用；账号 ID、版本、禁用状态保持；任务列表总数保持；持久设置 `maxRetries:2` 保持|
|空目录二次文件重放|未执行|首次双账号模型验证未通过，不重复制造无意义失败|
|资源/秘密清理|通过|临时 Key 撤销，服务和本次浏览器退出，临时 `instance-*` 目录删除，保留加密源文件及脱敏记录|

机器报告中的 `checks.management:true` 仅指该运行已完成的基础管理断言，不表示所有管理能力已完成真实验收；`status:partial_or_failed` 才是总体结果。`checks.restart:true` 对应当时已执行的检查，不夸大为所有任务正文逐字段重启比较。

### 主记录关联

|账号|导入任务 ID|验证请求 ID|实际结果|
|---|---|---|---|
|A|`task_1627d550-3c67-4a97-b959-4fda0d091887`|`verify_a72f554c-7978-4fdc-817e-0ed2c1732609`|取消；未生成；上游模型状态未知|
|B|`task_0d061b85-d25a-4055-bfd5-25516e45cc91`|`verify_8cb00bff-b9d4-44f0-b37d-c690f283fd84`|取消；未生成；上游模型状态未知|

A/B 各有两次后台 XHR 401 和一次后台 XHR 403。归属由顺序执行的导入任务和独立浏览器事件关联；没有将后台错误填入模型 `upstreamStatus`。

## 可复用实现与回归

- 新增 `scripts/tests/managementLive.js`、`scripts/tests/live/credentialBridge.py`、真实子进程 `serverWorker.js` 及 `evidence.js`。使用说明见 [本机真实凭证验收](management-api-live-testing.md)。
- 入口强制 loopback、禁止 HTTP 自动重定向；秘密走受控内存/本机请求，不入命令参数或报告。生成请求发送前计数，跨实例共享最多 12 次预算。
- 新增独立离线测试 `managementLiveHarness.test.js`，覆盖确定性双账号选取、外部目标拒绝与证据门槛拒绝。它是离线单元测试，不是 LIVE 成功证据。
- 修正 `ProxyServerSystem.start()`：当全部凭证禁用或待验证时，不再回退加载全部凭证。新增启动/重启回归；实际本次重启也没有加载两份待验证凭证。
- `--require-full --live-evidence ...` 校验当前业务源码摘要、双账号/请求归属、重放、日志摘要、请求预算、隔离性和清理。对本次失败报告实际执行，**按预期 exit 1**；失败点为 LIVE-01 和 full gate，不存在放行。
- 完整本地测试增加为 22 个脚本；真实测试不加入默认测试运行器，避免普通回归消费真实账号。
- 在真实运行结束后进一步补强入口：快照有界等待、并发请求异常收尾、B 实际验证完成期间 A 仍在运行才计重叠、重启逐任务结果比较、撤销失败仍关闭资源。这些新增成功分支未获真实账号通过，不能追记到历史报告。

## 证据位置与保留范围

以下路径相对集成工作区，均为本地文件；运行数据位于 Git 忽略目录，不随代码提交：

- [主机器结果](../data/management-live/run-EGxBQI/result.json)
- [主过程 JSONL](../data/management-live/run-EGxBQI/events.jsonl)
- [首次超时结果](../data/management-live/run-LgREXW/result.json)
- [首次过程 JSONL](../data/management-live/run-LgREXW/events.jsonl)
- [无界面页面诊断](../data/management-live/diagnosis-1790064749886.json)

主记录业务源码 SHA-256：`37495996f9e9c8ca1cfb79020feb7206f01a8fa59b2069cfa2b65ab376a3b878`。

主过程文件 SHA-256：`bee684c83604cd22def70c50776654bdb418af416e18edd1cf9dd2914ebf04d6`。

报告不保存 Cookie 值、密码、API Key、任意上游响应正文或邮箱。检查时 7860/9998 无残留监听，本次 Camoufox 已退出。删除的是本次实例产生的明文凭证与运行数据，未保留其明文恢复副本；原加密文件仍可用于重新导入。下载的浏览器保留供后续复验。

## 后续必须解决的阻塞

1. 在另行授权的源端流程确认 A/B 当前真实会话是否有效；若需登录或身份挑战，按正规流程完成并重新导出，不在本轮偷偷重新登录或换账号。
2. 在已确认有效的会话上核实可信身份字段。若现有页面不再提供该字段，修复身份适配并先过模拟回归；不能用 Cookie 存在或页面任意邮箱代替当前身份。
3. 检查应用预览初始化及后台授权失败的原因。页面 HTTP 200 并不排除后台权限或网络问题。
4. 重新对两个固定不同账号执行真实导入、模型复验、自动启用、模型接口、重叠保护、重启与空目录重放，拿到完整证据后才可能放行 LIVE。

本轮停止于上述边界，未声称双账号可用、模型可用或线上已生效。

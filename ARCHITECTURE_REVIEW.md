# 多账号轮询架构复盘

## 结论

当前项目不适合直接改成纯 Go：核心能力依赖 Playwright 浏览器上下文、页面内 WebSocket 和 AI Studio 的网页协议。Go 可以把 HTTP 网关、账号调度和管理后台做得更轻，但仍需要保留浏览器/页面桥接层；直接重写会带来协议兼容、登录态和流式响应回归风险。

## 与参考实现的关键差异

| 维度     | 当前项目原实现                                          | 本次调整后的实现                                                   |
| -------- | ------------------------------------------------------- | ------------------------------------------------------------------ |
| 账号承载 | 多个浏览器上下文，但请求容易受全局 current account 影响 | 每个账号独立上下文、WebSocket 和保活 Worker                        |
| 轮询含义 | 按活跃连接做简单请求轮询                                | 按请求分配；轮询只做同负载时的 tie-breaker，长流优先避开高在途负载 |
| 429 处理 | 触发切换/重试，但账号仍可能很快再次被选中               | 记录模型级冷却，并触发账号级 quota 熔断；由 AutoHeal 隔离探测恢复  |
| 重试     | 请求级绑定已有基础                                      | 重试只选择其他可调度账号，避免回到正在冷却的账号                   |
| 可观测性 | 能看到上下文和连接                                      | 账号列表显示 429 冷却/在途请求数，并支持单账号连接测试             |
| 测试操作 | 主要依赖日志或实际请求                                  | “测试账号连接”只检查 Auth、页面和 WebSocket，不生成模型内容        |
| 保活     | 部分逻辑跟随当前页面                                    | HealthMonitor 与 Rocket/Launch Handler 都按账号运行                |

## 使用者视角的行为

以 5 个账号为例，配置 `MAX_CONTEXTS=5` 后：

1. 服务启动第一个可用账号，其余账号在后台加载。
2. 每个账号建立独立浏览器上下文和 WebSocket 后才进入调度池。
3. 第 1、2、3、4、5 个新请求会按可用账号分配；如果某个账号正在处理长流，则优先分配给在途请求更少的账号。
4. 某账号返回 429 后，记录该账号与原始模型的退避信息；当前版本同时将整个凭证暂时标记为 `quota_exhausted` 并关闭其上下文。
5. 当前请求转向其他健康凭证；没有可用凭证时返回 429，不再把请求硬塞给已限流账号。
6. 默认 20 分钟窗口结束后，AutoHeal 使用上下文池之外的隔离浏览器探测；健康才重新启用，失败则保留禁用并在后续周期继续探测。
7. 在账号列表点击“测试”只检查 Auth、页面和 WebSocket，不生成模型内容，也不会清除尚未到期的模型冷却。

## 为什么不把“build”当成保活

前端 build 只生成静态资源。浏览器上下文、WebSocket、冷却状态和保活 Worker 都属于长期运行的 Node 进程，因此生产环境需要持续运行 `npm start`；Windows 用任务计划程序/服务托管，Docker 使用 `restart: unless-stopped`。

## 后续可选演进

如果后续账号规模从 5 扩展到几十或几百，建议抽出一个独立的调度层，持久化账号健康、冷却、并发和 quota 状态，再让 Node 浏览器桥接作为 Provider。当前 5 账号场景下，继续使用 Node + Playwright 的改造成本和回归风险更低。
